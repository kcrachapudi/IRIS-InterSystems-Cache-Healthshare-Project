version: '3.8'

services:
  iris:
    image: intersystemsdc/iris-community:latest
    container_name: iris-simple
    ports:
      - "52773:52773"
      - "1972:1972"
    environment:
      - IRIS_USERNAME=SuperUser
      - IRIS_PASSWORD=SYS
    restart: always